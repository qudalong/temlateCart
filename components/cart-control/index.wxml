<view class="operation">
  <view wx:if="{{goodCount>0}}" class="iconfont icon-jian" bindtap='minus'></view>
  <view wx:if="{{goodCount>0}}" class='count'>{{goodCount}}</view>
  <view class='iconfont icon-jia' bindtap='add'></view>
</view>